const runtimeConfig = {
  public: {
    siteName: "Nuxt.js TypeScript Boilerplate",
    cdn: "https://ik.imagekit.io/2gweb/piovega/",
    // baseURL: process.env.ENDPOINT,
    // appName: process.env.APP_NAME,
    // token: process.env.TOKEN,
  },
};

export default runtimeConfig;
